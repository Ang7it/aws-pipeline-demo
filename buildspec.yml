version: 0.2

phases:
  pre_build:
    commands: echo In pre build phase
    
  build:
    commands:
    - echo Entered build phase
    - mvn clean package
    
  post_build:
    commands:
      -echo Build completed successfully
        
artifacts:
  files:
    -target/aws-codedeploy.jar
  discard-paths: yes        
        